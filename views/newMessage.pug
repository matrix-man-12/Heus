extends layouts/main-layout.pug  

block content
    .chatPageContainer  
        .chatTitleBar
            label(for="userSearchTextbox") To:
            #seletedUsers   
                input#userSearchTextbox(type="text", placeholder="Type name of the person")
        .resultsContainer
        button#createChatButton(disabled) Create chat